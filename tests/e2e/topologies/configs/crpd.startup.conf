system {
    host-name crpd-clab;
    root-authentication {
        encrypted-password "$6$xy$..clab default..";
    }
    services {
        ssh {
            root-login allow;
        }
        netconf {
            ssh;
        }
    }
}
