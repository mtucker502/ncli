# ncli end-to-end harness

This directory contains the containerlab-driven end-to-end test suite.
It is opt-in (`pytest -m clab`) and requires Docker + containerlab + the
three vendor images on either the local host or a remote SSH-reachable host.

Full prerequisites and run instructions arrive in Phase 11. Until then, this
directory is under construction.
