# NCLI Config Blocklist
# Regex patterns, one per line. Lines starting with # are comments.
# These patterns are checked against config commands before pushing.

# Dangerous config commands
^no (shutdown|enable)
^crypto key zeroize
^license boot
